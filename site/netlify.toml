[build]
publish = "output"

[build.lifecycle]
onBuild = "mkdir -p output && cp index.html output/index.html"
onPostBuild = "printenv | tr '\n' ' '"

[build.environment]
TEST_ENVIRONMENT_VARIABLE = "ECHO_THIS"